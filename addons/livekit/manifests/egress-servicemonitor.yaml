apiVersion: v1
kind: Service
metadata:
  name: livekit-egress-metrics
  namespace: livekit
  labels:
    app: livekit-egress
    component: metrics
spec:
  selector:
    app: livekit-egress
  ports:
    - name: metrics
      port: 7889
      targetPort: 7889
  clusterIP: None
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: livekit-egress
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - livekit
  selector:
    matchLabels:
      app: livekit-egress
      component: metrics
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
