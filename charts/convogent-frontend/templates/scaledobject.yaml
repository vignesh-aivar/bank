{{- if and .Values.frontend.enabled ((.Values.frontend.keda).enabled) }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "convogent-frontend.name" . }}
  namespace: {{ include "convogent-frontend.namespace" . }}
  labels:
    {{- include "convogent-frontend.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: {{ include "convogent-frontend.name" . }}
  pollingInterval: {{ .Values.frontend.keda.pollingInterval | default 15 }}
  cooldownPeriod: {{ .Values.frontend.keda.cooldownPeriod | default 300 }}
  minReplicaCount: {{ .Values.frontend.keda.minReplicas | default 1 }}
  maxReplicaCount: {{ .Values.frontend.keda.maxReplicas | default 3 }}
  advanced:
    restoreToOriginalReplicaCount: false
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.frontend.keda.scaleDownStabilization | default 300 }}
          policies:
            - type: Pods
              value: 1
              periodSeconds: 60
          selectPolicy: Min
        scaleUp:
          stabilizationWindowSeconds: 0
          policies:
            - type: Percent
              value: 100
              periodSeconds: 15
          selectPolicy: Max
  triggers:
    {{- toYaml .Values.frontend.keda.triggers | nindent 4 }}
{{- end }}
