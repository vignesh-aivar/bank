{{- if .Values.frontend.serviceAccount.create | default .Values.global.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.frontend.serviceAccountName | default .Values.global.serviceAccountName }}
  namespace: {{ include "convogent-frontend.namespace" . }}
  labels:
    {{- include "convogent-frontend.labels" . | nindent 4 }}
  {{- with .Values.frontend.serviceAccount.annotations | default .Values.global.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
