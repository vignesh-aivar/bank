{{- if and .Values.chatService.enabled ((.Values.chatService.externalSecret).enabled) }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.chatService.externalSecret.name }}
  namespace: {{ include "convogent-chat.namespace" . }}
  labels:
    {{- include "convogent-chat.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.chatService.externalSecret.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.chatService.externalSecret.secretStoreRef | default "aws-secrets-manager" }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.chatService.externalSecret.name }}
    creationPolicy: Owner
  dataFrom:
    {{- range .Values.chatService.externalSecret.dataFrom }}
    - extract:
        key: {{ .key }}
    {{- end }}
{{- end }}
