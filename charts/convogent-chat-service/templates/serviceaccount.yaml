{{- if .Values.chatService.serviceAccount.create | default .Values.global.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.chatService.serviceAccountName | default .Values.global.serviceAccountName }}
  namespace: {{ include "convogent-chat.namespace" . }}
  labels:
    {{- include "convogent-chat.labels" . | nindent 4 }}
  {{- with .Values.chatService.serviceAccount.annotations | default .Values.global.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
