{{- if .Values.voiceService.serviceAccount.create | default .Values.global.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.voiceService.serviceAccountName | default .Values.global.serviceAccountName }}
  namespace: {{ include "convogent-voice.namespace" . }}
  labels:
    {{- include "convogent-voice.labels" . | nindent 4 }}
  {{- with .Values.voiceService.serviceAccount.annotations | default .Values.global.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
