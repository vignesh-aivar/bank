{{- if and .Values.voiceService.enabled ((.Values.voiceService.externalSecret).enabled) }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.voiceService.externalSecret.name }}
  namespace: {{ include "convogent-voice.namespace" . }}
  labels:
    {{- include "convogent-voice.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.voiceService.externalSecret.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.voiceService.externalSecret.secretStoreRef | default "aws-secrets-manager" }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.voiceService.externalSecret.name }}
    creationPolicy: Owner
  dataFrom:
    {{- range .Values.voiceService.externalSecret.dataFrom }}
    - extract:
        key: {{ .key }}
    {{- end }}
{{- end }}
