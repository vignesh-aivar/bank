{{- if and .Values.pcaService.enabled ((.Values.pcaService.externalSecret).enabled) }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.pcaService.externalSecret.name }}
  namespace: {{ include "convogent-pca.namespace" . }}
  labels:
    {{- include "convogent-pca.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.pcaService.externalSecret.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.pcaService.externalSecret.secretStoreRef | default "aws-secrets-manager" }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.pcaService.externalSecret.name }}
    creationPolicy: Owner
  dataFrom:
    {{- range .Values.pcaService.externalSecret.dataFrom }}
    - extract:
        key: {{ .key }}
    {{- end }}
{{- end }}
