{{- if .Values.evalService.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "convogent-eval.name" . }}-config
  namespace: {{ include "convogent-eval.namespace" . }}
  labels:
    {{- include "convogent-eval.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.evalService.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
