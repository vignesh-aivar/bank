{{- if and .Values.evalService.enabled ((.Values.evalService.externalSecret).enabled) }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.evalService.externalSecret.name }}
  namespace: {{ include "convogent-eval.namespace" . }}
  labels:
    {{- include "convogent-eval.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.evalService.externalSecret.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.evalService.externalSecret.secretStoreRef | default "aws-secrets-manager" }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.evalService.externalSecret.name }}
    creationPolicy: Owner
  dataFrom:
    {{- range .Values.evalService.externalSecret.dataFrom }}
    - extract:
        key: {{ .key }}
    {{- end }}
{{- end }}
